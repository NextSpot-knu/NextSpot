# NextSpot API 개발/CI 의존성 — 런타임(requirements.txt) 위에 얹어 설치한다.
#   로컬:  pip install -r requirements.txt -r requirements-dev.txt
#   CI:    .github/workflows/ci.yml 의 api 잡이 이 파일을 사용한다.
# (Dockerfile 프로덕션 이미지는 requirements.txt 만 설치 — 테스트 도구를 이미지에 넣지 않는다.)

pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
