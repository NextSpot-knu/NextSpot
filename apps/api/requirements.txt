# NextSpot API 런타임 의존성 (로컬 전용).
#   로컬 구동:  pip install -r requirements.txt  →  uvicorn app.main:app
#   Dockerfile 도 이 파일로 pip install 한다(poetry 미사용).
#   의존성 명세 참고본은 pyproject.toml 과 수동 동기화한다.

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic[email]>=2.6.4
pydantic-settings>=2.2.1
pyjwt[crypto]>=2.8.0
supabase>=2.3.0
python-dotenv>=1.0.1
httpx>=0.27.0
structlog>=24.1.0
scikit-learn>=1.4.0
joblib>=1.3.0
