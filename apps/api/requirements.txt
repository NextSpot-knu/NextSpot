# NextSpot API 런타임 의존성 (로컬 전용).
#   로컬 구동:  pip install -r requirements.txt  →  uvicorn app.main:app
#   Dockerfile 도 이 파일로 pip install 한다(poetry 미사용).
#   이 파일이 의존성 정본이다(pyproject.toml 은 ruff 등 도구 설정 전용).
#   테스트/린트 도구는 requirements-dev.txt (pytest 로 테스트하려면 dev 파일도 설치할 것).

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic[email]>=2.6.4
pydantic-settings>=2.2.1
pyjwt[crypto]>=2.8.0
supabase>=2.3.0
python-dotenv>=1.0.1
httpx>=0.27.0
structlog>=24.1.0
scikit-learn>=1.4.0
